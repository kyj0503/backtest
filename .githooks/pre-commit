#!/usr/bin/env bash
# Pre-commit hook: build, test, lightweight run
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "[pre-commit] Running verification script..."
scripts/verify-before-commit.sh

echo "[pre-commit] Verification passed. Proceeding with commit."


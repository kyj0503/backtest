# Example environment variables for backend (do NOT commit real secrets)
# Copy this file to `.env` and fill in real values before running the service.

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Logging
LOG_LEVEL=INFO

# Security
# Replace with a secure random string in production
SECRET_KEY=replace-with-secret

# Backtesting defaults
DEFAULT_INITIAL_CASH=10000.0
MAX_BACKTEST_DURATION_DAYS=3650
DEFAULT_COMMISSION=0.002

# External API
YAHOO_FINANCE_TIMEOUT=30

# 최적화 설정
MAX_OPTIMIZATION_ITERATIONS=1000
OPTIMIZATION_TIMEOUT_SECONDS=300

# 네이버 검색 API
NAVER_CLIENT_ID=your_naver_client_id_here
NAVER_CLIENT_SECRET=your_naver_client_secret_here

# 뉴스 정보는 네이버 검색 API 사용

# CORS origins (comma separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# === MySQL connection (example for Docker Desktop on Windows)
# Use host.docker.internal so containers can reach the host MySQL instance.
# Replace placeholders with your credentials. Do NOT commit the real .env to git.
DATABASE_HOST=host.docker.internal
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=your_db_password_here
DATABASE_NAME=stock_data_cache

# Optionally supply a full SQLAlchemy URL which the app will prefer if present.
# Example: mysql+pymysql://user:password@host:3306/dbname?charset=utf8mb4
DATABASE_URL=

services:
  backend:
    build: ./backend
    ports:
      - "8001:8000"
    environment:
      - PYTHONPATH=/app
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "8081:80"
    depends_on:
      - backend

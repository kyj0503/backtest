version: '3.8'

services:
  backend:
    image: backtest-backend:latest
    ports:
      - "8001:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    image: backtest-frontend:latest
    ports:
      - "8082:80"
    depends_on:
      - backend
    restart: unless-stopped

# Monitoring Environment Configuration
# Copy this file to .env in the monitoring directory

# Grafana Credentials (Change these for production!)
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# Ports (Avoid conflicts with other services)
GRAFANA_PORT=3001
PROMETHEUS_PORT=9090

# P2-22: requirements-test.txt의 완전 고정 락. requirements.lock.txt 위에
# "얹어서" 설치되는 것을 전제로 하며 (Dockerfile의 test 스테이지는 builder를
# FROM하고, Dockerfile.dev는 requirements.lock.txt 설치 후 이 파일을 설치),
# 그래서 requirements.lock.txt에 이미 있는 패키지는 여기 다시 나열하지 않는다.
#
# 생성 방법은 requirements.lock.txt와 동일 (같은 pip freeze 출력에서, 어떤
# 설치된 패키지도 필요로 하지 않는/오직 pytest 계열만 필요로 하는 패키지만
# `pip show`의 Required-by로 골라냈다). httpx/httpcore/sniffio는 FastAPI
# TestClient 등 테스트에서만 쓰이고 애플리케이션 코드 경로에는 전혀
# 등장하지 않는다 (requirements.lock.txt의 anyio/starlette 쪽에는 sniffio가
# 필요하지 않음을 pip show로 확인했다).

coverage==7.15.2
factory-boy==3.3.0
Faker==22.6.0
freezegun==1.4.0
httpcore==1.0.9
httpx==0.26.0
iniconfig==2.3.0
pluggy==1.6.0
pytest==8.4.2
pytest-asyncio==0.24.0
pytest-cov==4.1.0
pytest-mock==3.12.0
sniffio==1.3.1
